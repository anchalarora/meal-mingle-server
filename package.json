{
  "name": "meal-mingle-server",
  "version": "1.0.0",
  "description": "A Node.js web server that retrieves data from the Swiggy API and provides it to the MealMingle application.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "parcel build server.js --target node --out-dir dist"
  },
  "repository": {
    "type": "git"
  },
  "author": "Anchal Arora",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
